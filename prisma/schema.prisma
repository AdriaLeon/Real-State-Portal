// TODO: add RawListing and Listing models (see dev plan, step 2 — schema)

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}
